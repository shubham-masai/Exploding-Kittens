
const initialState = {
    username: null,
    score: null,
    isLoading: false,
    isError: false,
    ErrorMsg: "",
    allUser: []
}

export const userReducer = (state = initialState, { type, payload }) => {
    switch (type) {
    
        default: return state
    }
}